# API Configuration (without /api suffix, it's added in the code)
VITE_API_URL=http://localhost:3001

# For Vercel deployment, set to your backend URL (without /api suffix)
# VITE_API_URL=https://brin-api.onrender.com